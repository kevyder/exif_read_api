# Image EXIF read API

## Getting Started

Build docker image

```bash
docker compose build
```

Run application

```bash
docker compose up
```

Run Tests

```bash
docker compose run --rm app coverage run -m pytest
```
